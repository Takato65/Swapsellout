{
  "name": "swapping-sellout",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tunnel": "cloudflared tunnel --url http://localhost:5173/"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.5.0"
  }
}
